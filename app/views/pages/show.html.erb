
<p>
  <strong>Name:</strong>
  <%= @page.name %>
</p>

<p>
  <strong>Webclip:</strong>
  <%= @page.webclips.map(&:name).join(', ') %>
</p>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
