
<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Webclip:</strong>
  <%= @category.webclips.map(&:name).join(', ') %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
