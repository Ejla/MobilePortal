  <style type="text/css" media="screen">
    body.frontend.<%= PortalSetting.first.theme.present? ? PortalSetting.first.theme : 'default' %>_theme {
    background-color: <%= PortalSetting.first.bg_css %>;
    font-family: <%= escape_javascript(PortalSetting.first.font).html_safe %>;
  }
  
  </style>
