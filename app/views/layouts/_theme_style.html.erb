  <style type="text/css" media="screen">
    body.frontend.<%= Portal.first.theme.present? ? Portal.first.theme : 'default' %>_theme {
    background-color: <%= Portal.first.bg_css %>;
    font-family: <%= escape_javascript(Portal.first.font).html_safe %>;
  }
  
  </style>
